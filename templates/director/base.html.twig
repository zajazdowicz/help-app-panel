<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Panel dyrektora - HelpDreams{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
            padding-top: 20px;
        }
        .director-container {
            display: flex;
            flex-wrap: wrap;
            min-height: calc(100vh - 100px);
        }
        .director-sidebar {
            width: 250px;
            background: #fff;
            border-right: 1px solid #dee2e6;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0,0,0,0.05);
        }
        .director-content {
            flex: 1;
            padding: 20px;
            max-width: calc(100% - 250px);
        }
        .sidebar-heading {
            font-size: 1.2rem;
            font-weight: 600;
            color: #495057;
            margin-bottom: 1.5rem;
            padding-bottom: 10px;
            border-bottom: 2px solid #0d6efd;
        }
        .sidebar-nav .nav-link {
            color: #495057;
            padding: 10px 15px;
            border-radius: 8px;
            margin-bottom: 5px;
            display: flex;
            align-items: center;
            transition: all 0.2s;
        }
        .sidebar-nav .nav-link i {
            margin-right: 10px;
            width: 20px;
            text-align: center;
        }
        .sidebar-nav .nav-link:hover,
        .sidebar-nav .nav-link.active {
            background-color: #e7f1ff;
            color: #0d6efd;
        }
        .orphanage-badge {
            background-color: #0d6efd;
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.9rem;
            margin-bottom: 20px;
            display: inline-block;
        }
        @media (max-width: 768px) {
            .director-sidebar {
                width: 100%;
                border-right: none;
                border-bottom: 1px solid #dee2e6;
            }
            .director-content {
                max-width: 100%;
            }
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    {% include 'partials/_navbar.html.twig' ignore missing %}
    {% if not app.user or not is_granted('ROLE_DIRECTOR') %}
        <div class="container mt-5">
            <div class="alert alert-danger">
                Brak dostępu. Ta strona jest przeznaczona tylko dla dyrektorów domów dziecka.
            </div>
        </div>
    {% else %}
        <div class="container-fluid director-container">
            <div class="director-sidebar">
                <div class="sidebar-heading">
                    <i class="bi bi-person-badge"></i> Panel dyrektora
                </div>
                {% if app.user.orphanage %}
                    <div class="orphanage-badge">
                        <i class="bi bi-house-door"></i> {{ app.user.orphanage.name }}
                        {% if not app.user.orphanage.isVerified %}
                            <span class="badge bg-warning ms-1">Oczekuje na weryfikację</span>
                        {% endif %}
                    </div>
                {% else %}
                    <div class="alert alert-warning small">
                        {% if is_granted('ROLE_ADMIN') and is_granted('ROLE_DIRECTOR') %}
                            <strong>Super Admin</strong><br>
                            Jesteś zalogowany jako Super Admin. Nie masz przypisanego domu dziecka, więc nie możesz dodawać dzieci ani marzeń.
                        {% else %}
                            Nie jesteś przypisany do żadnego domu dziecka.
                            <a href="{{ path('director_orphanage_register') }}" class="alert-link">Zarejestruj dom dziecka</a>
                        {% endif %}
                    </div>
                {% endif %}
                <nav class="sidebar-nav">
                    <a class="nav-link {{ app.request.get('_route') starts with 'director_child' ? 'active' }}" href="{{ path('director_child_index') }}">
                        <i class="bi bi-people"></i> Dzieci
                    </a>
                    <a class="nav-link {{ app.request.get('_route') starts with 'director_dream' ? 'active' }}" href="{{ path('director_dream_list') }}">
                        <i class="bi bi-stars"></i> Nasze marzenia
                    </a>
                    <a class="nav-link {{ app.request.get('_route') == 'director_child_new' ? 'active' }}" href="{{ path('director_child_new') }}">
                        <i class="bi bi-person-plus"></i> Dodaj dziecko
                    </a>
                    <a class="nav-link {{ app.request.get('_route') == 'director_dream_new' ? 'active' }}" href="{{ path('director_dream_new') }}">
                        <i class="bi bi-gift"></i> Dodaj marzenie
                    </a>
                    <div class="mt-4 pt-3 border-top">
                        <a class="nav-link" href="{{ path('app_dream_index') }}">
                            <i class="bi bi-binoculars"></i> Przeglądaj wszystkie marzenia
                        </a>
                        <a class="nav-link" href="{{ path('app_home') }}">
                            <i class="bi bi-house"></i> Strona główna
                        </a>
                    </div>
                </nav>
            </div>
            <div class="director-content">
                {% for flash in app.flashes('success') %}
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        {{ flash }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
                {% for flash in app.flashes('warning') %}
                    <div class="alert alert-warning alert-dismissible fade show" role="alert">
                        {{ flash }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
                {% for flash in app.flashes('error') %}
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        {{ flash }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
                {% block director_body %}{% endblock %}
            </div>
        </div>
    {% endif %}
    <footer class="footer mt-5 py-4 bg-light border-top">
        <div class="container text-center">
            <p class="mb-0 text-muted">&copy; {{ "now"|date("Y") }} HelpDreams. Wszelkie prawa zastrzeżone.</p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
