{% extends 'base.html.twig' %}

{% block title %}HelpDreams - Strona główna{% endblock %}

{% block body %}
    <div class="text-center py-5">
        <h1 class="display-4">{{ message }}</h1>
        <p class="lead">To jest przykładowa strona główna aplikacji Symfony.</p>
        <p class="text-muted">Sprawdź, czy routing działa poprawnie.</p>

        {% if app.user %}
            <div class="mt-4">
                <p class="fs-5">Zalogowany jako: <strong>{{ app.user.email }}</strong></p>
                <p class="text-muted">Twoje role: <code>{{ app.user.roles|join(', ') }}</code></p>
            </div>
        {% else %}
            <div class="mt-5">
                <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg me-3">Zarejestruj się</a>
                <a href="{{ path('app_login') }}" class="btn btn-outline-primary btn-lg">Zaloguj</a>
            </div>
        {% endif %}
    </div>
{% endblock %}
